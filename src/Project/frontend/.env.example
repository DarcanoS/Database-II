# Example environment variables for the frontend (Vite)
# Copy this file to `.env` or `.env.local` and change values before running the app.

# Base URL used by the frontend HTTP client. Must include protocol and prefix path to API.
# Example: `http://localhost:8000/api/v1` or `https://api.example.com/api/v1`
VITE_API_BASE_URL=http://localhost:8000/api/v1

# Optional: Application title used in the UI (keeps UI text configurable)
VITE_APP_TITLE=Air Quality Dashboard

# Optional: Enable or disable client-side mocking (true/false)
# If the project includes mock logic, set this to true to enable it.
VITE_ENABLE_MOCK=false
